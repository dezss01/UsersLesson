<%= provide :page_title, 'Questions' %>
<%= currently_at t('menu.questions') %>

<div class="container">
  <div class="mt-3">
    <%== pagy_bootstrap_nav @pagy %>
  </div>

  <%= render @questions %>

  <div class="mt-3">
    <%== pagy_bootstrap_nav @pagy %>
  </div>
</div>